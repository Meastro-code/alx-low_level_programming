#include <ctype.h>
/**
 * _islower - checks if character is lowercase
 * @c: The character to check
 *
 * Return: On success 1.
 * On error, -1 is returned
 */

int _islower(int c) {
if (c >= 'a' && c <= 'z')
{
return (1); 
}
else
{
return (0);
}
}
