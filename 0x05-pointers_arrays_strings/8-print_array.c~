/**
 * print_array - prints n of an array of int
 * @a: pointer to the first element
 * @n: number of elements to be printed
 *
 * This function takes a pointer
 * to an integer and an integer as its parameters
 * and prints the first n elements
 * of the array pointed to by a
 */
void print_array(int *a, int n)
{
int i;
for (i = 0; i < n; i++)
{
printf("%d", a[i]);
if (i != n - 1)
{
printf(", ");
}
}
printf("\n");
}
